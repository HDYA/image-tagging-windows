# Face recognition module - optional
# For AMD 7900XTX (ROCm on Linux) or DirectML (Windows):
#   pip install onnxruntime-directml>=1.17.0   # Windows DirectML (AMD GPU)
#   pip install onnxruntime-rocm>=1.17.0       # Linux ROCm (AMD GPU)
# Fallback CPU:
#   pip install onnxruntime>=1.17.0

insightface>=0.7.3
opencv-python-headless>=4.9.0
numpy>=1.24.0